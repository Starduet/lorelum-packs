---
anti_patterns:
  - description: Improvised titles and commit subjects make history unscannable, break changelog tooling, and leave later readers unable to find why a change was made.
    id: issue-pr-etiquette.pull-request.untyped-improvised-history
    name: Untyped, improvised commit history
    severity: warn
applies_when: naming a commit or the PR title in a repository whose history follows a typed conventional format, or whose contributing guide names one
id: issue-pr-etiquette.pull-request.title-and-commit-conventionally
severity: warn
stage: pull-request
tech_stack:
  - github-contribution
title: Title and Commit Conventionally
---

## When to apply

Apply when writing any commit message or PR title on a repository that declares a commit convention
— most often Conventional Commits. The decision is the exact form of the subject line and what
belongs in the body instead. Do not apply where the repository explicitly follows a different naming
style, and then follow that style.

## Guidance

Follow the declared convention exactly: type, optional scope, colon, then an imperative, lowercase
subject within the convention's length budget — commonly seventy-two characters — stating what the
change does, not the story of making it. One logical change per commit; put the why, the trade-offs,
and links to issues in the commit body or the PR body, not the subject. When the repository
squash-merges, the PR title becomes the commit, so the same rule governs it. Stop when
`log --oneline` reads as a changelog of what changed, without opening a single diff.

## Anti-pattern

A merged history reads "fixes", "update stuff", "final changes", "wip". Six months later, a
contributor greps for the change that altered pack validation and finds nothing; the changelog
script that parses typed subjects emits an empty release; blame points at a subject that could
describe half the repository.

## Why

History is a search interface. Typed, imperative, scoped subjects let people and tooling find
changes by kind and area, generate release notes, and read a repository's evolution without opening
diffs; improvised subjects erase that index at zero benefit to the author.

## Exceptions and boundaries

Follow the repository's declared variant where it differs — different type sets, upper-case
subjects, or ticket prefixes override the generic form. Merge commits generated by the platform
carry their own format and are not authored subjects. Work-in-progress commits on a private branch
may be informal; the convention binds the history others will read, so squash or reword before
opening the PR when the repository's flow expects it.

## Example

A PR adding a resource protocol to a pack engine is titled
`feat(pack): add Pack-native resource support` — type, scope, imperative lowercase subject,
sixty-one characters. Its body carries the design rationale and the linked issue. A sibling typo fix
lands as `docs(readme): fix install command flag`, and a year later both are found by
`git log --grep 'resource support'`.
